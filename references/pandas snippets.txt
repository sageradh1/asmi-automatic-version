When we need to select row with certain condition:
	search = df[ df['column_name']== somevalue ]

But if we need to check if the value is in a list:
	search = df[ df["column_name"].isin(listname) ]


To add new column with a general function:
	def value_in_column(row):
	    value =row['column_a']+row['column_b']
	    return value

	df['new_column_name']=anyalreadyexistingdf.apply(value_in_column,axis=1)




To copy a df
# newdf=asmidf[['video_id']].copy()
